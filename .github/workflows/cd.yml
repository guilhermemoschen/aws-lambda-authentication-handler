name: Continuous Delivery

on:
  release:
    types: [published]

jobs:
  Publish:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Sandbox 
      run: |
        TAG=$(echo $GITHUB_REF | cut -c 9-)
        echo "Deploying to production server on branch $TAG"
